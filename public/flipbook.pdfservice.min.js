/* lightweight stub to satisfy Real3D Flipbook expectations */
(function(){
  var root = (typeof window !== 'undefined' ? window : globalThis);
  root.FLIPBOOK = root.FLIPBOOK || {};
  root.FLIPBOOK.PDFSERVICE = root.FLIPBOOK.PDFSERVICE || {};
  if (!root.FLIPBOOK.PDFSERVICE.BLANK) {
    root.FLIPBOOK.PDFSERVICE.BLANK = { type: 'blank' };
  }
})();